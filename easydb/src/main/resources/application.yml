spring:
  datasource:
    platform: h2
#    data: classpath:data.sql 需要从.sql文件生成时再指定,还需要把ddl-auto： none
#    schema: classpath:schema.sql
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
logging:
  level:
    root: warn
    org:
      hibernate: info
